# Uncomment the next line to define a global platform for your project
  platform :ios, '13.0'

target 'Firebase Realtime Database' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Firebase Realtime Database

  target 'Firebase Realtime DatabaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Firebase Realtime DatabaseUITests' do
    inherit! :search_paths
    # Pods for testing
  end
  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Alamofire'
  pod 'SVProgressHUD'
end
